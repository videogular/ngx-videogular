import { VgCuePointsDirective } from './vg-cue-points.directive';

describe('VgCuePointsDirective', () => {
  it('should create an instance', () => {
    const directive = new VgCuePointsDirective();
    expect(directive).toBeTruthy();
  });
});
