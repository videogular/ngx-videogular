import { VgMediaDirective } from './vg-media.directive';

describe('VgMediaDirective', () => {
  it('should create an instance', () => {
    const directive = new VgMediaDirective();
    expect(directive).toBeTruthy();
  });
});
