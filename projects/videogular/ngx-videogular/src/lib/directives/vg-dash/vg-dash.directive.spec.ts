import { VgDashDirective } from './vg-dash.directive';

describe('VgDashDirective', () => {
  it('should create an instance', () => {
    const directive = new VgDashDirective();
    expect(directive).toBeTruthy();
  });
});
